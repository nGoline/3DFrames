node_modules
dist
.DS_Store
*.local
.vite
tsconfig.tsbuildinfo

# Screenshots and scratch images. Nothing image-shaped belongs in this repo —
# the app generates its own geometry and ships no raster assets — so keeping
# them out wholesale is safer than remembering not to add them.
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.bmp
*.tiff
